name: weasis
title: Weasis
base: core24
version: '4.7.2'
summary: A free/libre/open medical DICOM viewer
description: |
  Weasis is a multipurpose standalone and web-based DICOM viewer with 
  a highly modular architecture. It is a very popular clinical viewer 
  used in healthcare by hospitals, health networks, multicenter research 
  trials, and patients.
  
  Note: With Snap, the .weasis directory is now located in /home/<username>/snap/weasis/current/.
  
  Key features:
  - Flexible integration to PACS, VNA, RIS, HIS or PHR
  - Responsive user interface and work well on high DPI screens
  - Web access through weasis protocol
  - Multi-language support
  - Configuration of preferences on server-side and client-side
  - API for building custom plug-ins
  - DICOM Send (storeSCU and STOW-RS)
  - DICOM Query/Retrieve (C-GET, C-MOVE and WADO-URI) and DICOMweb (QUERY and RETRIEVE)
  - Dicomizer module (allow importing standard images and convert them in DICOM)
license: Apache-2.0
grade: stable
confinement: strict
platforms:
  amd64:
    build-on: [amd64]
  arm64:
    build-on: [arm64]

plugs:
  desktop:
    mount-host-font-cache: false
  gtk-3-themes:
    interface: content
    target: $SNAP/data-dir/themes
    default-provider: gtk-common-themes
  icon-themes:
    interface: content
    target: $SNAP/data-dir/icons
    default-provider: gtk-common-themes
  sound-themes:
    interface: content
    target: $SNAP/data-dir/sounds
    default-provider: gtk-common-themes
  gnome-46-2404:
    interface: content
    target: $SNAP/gnome-platform
    default-provider: gnome-46-2404
  gpu-2404:
    interface: content
    target: $SNAP/gpu-2404
    default-provider: mesa-2404

layout:
  /usr/bin/lpr:
    bind-file: $SNAP/usr/bin/lpr

parts:
  remote-openjdk:
    plugin: dump
    source:
      - on amd64: https://github.com/adoptium/temurin26-binaries/releases/download/jdk-26.0.2%2B10/OpenJDK26U-jdk_x64_linux_hotspot_26.0.2_10.tar.gz
      - on arm64: https://github.com/adoptium/temurin26-binaries/releases/download/jdk-26.0.2%2B10/OpenJDK26U-jdk_aarch64_linux_hotspot_26.0.2_10.tar.gz
    source-checksum:
      - on amd64: sha256/56f768372f6ca1e2eb4c5f46b78f627949e8dcfe9c9723926cf45a45faf35802
      - on arm64: sha256/f82aeb1d5b0ddb93d3bd81c20948bc560c0758993157202424c17e805e2effbb
    organize:
      '*': java-runtime/
    override-stage: 'true'
    override-prime: 'true'

  remote-weasis-build:
    after: [remote-openjdk]
    plugin: dump
    stage-packages:
      - libc6
      - xdg-utils
      - zlib1g
      - libstdc++6
      - libgcc-s1
    #source: weasis-distributions/target/native-dist/weasis-native.zip
    source: https://github.com/nroduit/Weasis/releases/download/v4.7.2/weasis-native.zip
    source-checksum: sha256/463a5ea2db0f94bcaaf221134343fd9837c3b6829047b7c815274ac573ecbff0
    organize:
      '*': weasis-native/
    override-stage: 'true'
    override-prime: 'true'

  weasis:
    after: [remote-weasis-build]
    plugin: nil
    source: .
    source-type: local
    source-subdir: weasis-distributions
    override-build: |
      echo "Preparing Weasis build environment..."
      WEASIS_REMOTE=$(echo ${CRAFT_PART_INSTALL} | sed 's|parts/weasis/|parts/remote-weasis-build/|g')
      WEASIS_JDK=$(echo ${CRAFT_PART_INSTALL} | sed 's|parts/weasis/|parts/remote-openjdk/|g')
      cp -Rf ${WEASIS_REMOTE}/weasis-native ${CRAFT_PART_BUILD}/
      weasis-native/build/script/package-weasis.sh --output ${CRAFT_PART_INSTALL} --no-installer --jdk ${WEASIS_JDK}/java-runtime
      mv ${CRAFT_PART_INSTALL}/Weasis/* ${CRAFT_PART_INSTALL}/
      rm -rf ${CRAFT_PART_INSTALL}/Weasis
      craftctl default

apps:
  weasis:
    extensions: [gnome]
    command: bin/Weasis
    environment:
      _JAVA_OPTIONS: -Duser.home=$SNAP_USER_DATA
    plugs:
      - network
      - desktop
      - x11
      - home
      - network-bind
      - gsettings
      - opengl
      - avahi-control
      - mount-observe
      - removable-media
      - cups-control
  
  dicomizer:
    extensions: [gnome]
    command: bin/Dicomizer
    environment:
      _JAVA_OPTIONS: -Duser.home=$SNAP_USER_DATA
    plugs:
      - network
      - desktop
      - x11
      - home
      - network-bind
      - gsettings
      - opengl
      - avahi-control
      - mount-observe
      - removable-media
      - cups-control
